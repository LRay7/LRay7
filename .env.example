# Claude API key for the extraction step (https://platform.claude.com)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Ticketmaster Discovery API key (free tier) for the big-venue tier
TICKETMASTER_API_KEY=

# Where the SQLite DB lives (default: ./data/gighound.db)
# GIGHOUND_DB_PATH=
